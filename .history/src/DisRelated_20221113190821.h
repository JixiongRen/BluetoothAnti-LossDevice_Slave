#ifdef _DISRELATED_H
#define _DISRELATED_H
#include "math.h"
#include "stdio.h"
    typedef struct dis_arr{
        double DisList[10];
    }DA;

    double Discal(double, double, int);
    DA GaussianBlur(DA);
#endif